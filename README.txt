This is a simple program to draw the mandelbrot set.

It takes no command line args. To edit drawing parameters, edit the values defined in mandelbrot.cpp.

It outputs a file called mandelbrot.ppm.

As is, the program will generate a 18000x24000 pixel image. Included is this image in .png format.